# Dependencias del frontend

El frontend está desarrollado con React, por lo tanto no utiliza `requirements.txt`.
Las dependencias se gestionan desde `package.json`.

Para instalar todas las dependencias:

```bash
cd frontend
npm install
